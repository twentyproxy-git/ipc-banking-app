<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Nền trong suốt -->
    <solid android:color="@android:color/transparent"/>

    <!-- Border xung quanh -->
    <stroke
        android:width="2dp"
        android:color="#c7967d" />

    <!-- Bo góc 4 góc -->
    <corners android:radius="8dp" />
</shape>
