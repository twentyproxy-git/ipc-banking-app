<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Quyền truy cập Internet để tải bản đồ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Quyền vị trí để định vị người dùng -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IPCBanking">
        <activity
            android:name=".activities.MapActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDm3i47MIRG5TttHqzA9GpupdSB42p0SMU" />

        <activity
            android:name=".activities.WithdrawalActivity"
            android:exported="false" />
        <activity
            android:name=".activities.DepositActivity"
            android:exported="false" />
        <activity
            android:name=".activities.TransactionHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AddCustomerActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ReviewRequestActivity"
            android:exported="false" />
        <activity
            android:name=".activities.VerifyKYCActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AccountDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CustomerHomeActivity"
            android:exported="false" />
        <activity
            android:name=".activities.EditCustomerActivity"
            android:exported="false" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Main Activity -->
        <activity
            android:name=".activities.OfficerHomeActivity"
            android:exported="false" />
    </application>

</manifest>